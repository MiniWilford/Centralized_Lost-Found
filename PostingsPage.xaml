 // MainPage.xaml - UI Layout<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Centralized_Lost_Found.PostingsPage">

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="Start">
            <Button Text="☰" FontSize="20" BackgroundColor="Transparent" />
            <Label Text="Main Page – Lost Item Postings" FontSize="16" HorizontalOptions="CenterAndExpand"/>
            <StackLayout Orientation="Horizontal" HorizontalOptions="End">
                <Label Text="Inbox(1)" FontSize="14"/>
                <Image Source="inbox_icon.png" HeightRequest="20" WidthRequest="20" />
                <Label Text="Username" FontSize="14"/>
                <Image Source="profile_icon.png" HeightRequest="30" WidthRequest="30" />
            </StackLayout>
        </StackLayout>

        <!-- Add Item Button -->
        <Button Text="Don't see yours?: Add/+" 
                BackgroundColor="Red" 
                TextColor="White" 
                Grid.Row="1" 
                HorizontalOptions="Center"/>

        <!-- Filter Dropdown -->
        <Picker Title="Filter Locations Dropdown" Grid.Row="2" HorizontalOptions="Center" WidthRequest="200"/>

        <!-- Lost Item Grid -->
        <CollectionView Grid.Row="3">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="3" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="Gray" StrokeThickness="1" Padding="10" BackgroundColor="Transparent">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="5"/>
                        </Border.StrokeShape>
                        <Image Source="lost_item_placeholder.png" HeightRequest="100" WidthRequest="150"/>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>

